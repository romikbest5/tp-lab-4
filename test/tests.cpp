#include "gtest/gtest.h"
#include "../include/Automata.h"
 /* TEST(lab4, task_1) 
{
	Automata a;
	a.on();
	unsigned int coins = a.getCash();
	EXPECT_EQ(0, coins);
}

TEST(lab4, task_2) 
{
	Automata a;
	a.on();
	a.coin(50);
	a.choice(3);
	a.choice(3);

	unsigned int coins = a.getCash();
	EXPECT_EQ(0, coins);
}

TEST(lab4, task_3) 
{
	Automata a;
	a.on();
	a.coin(10);
	automata.choice(1);

	unsigned int coins = a.getCash();
	EXPECT_EQ(10, coins);
}

TEST(lab4, task_4) 
{
	Automata a;
	a.on();
	a.coin(100);
	a.choice(3);
	a.choice(2);
	a.choice(2);

	unsigned int coins = a.getCash();
	EXPECT_EQ(35, coins);
}

*/
